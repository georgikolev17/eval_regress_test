# Task Prompts

1. Fix Issue 1: update `count_up_to` so the sequence includes the upper bound.
2. Fix Issue 2: adjust `is_adult` so age 18 is treated as adult.
3. Fix Issue 3: make `greet` use the default guest name for empty string input.
4. Fix Issue 4: make `parse_key_value` correctly parse text when the value contains additional colons.
5. Fix Issue 5: correct `price_with_tax` to round to cents.
6. Fix Issue 6: refactor `dedupe_in_place` so it does not mutate the input list.
7. Fix Issue 7: handle empty input in `average` without raising an exception.
8. Fix Issue 8: correct filtering and ordering in `top_scores` to match its contract.
